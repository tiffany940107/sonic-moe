"""Placement and migration helpers for the public EP4 runtime prototype.

Stable benchmark paths live in ``static_greedy``, ``sonic_migration``, and
``windowed_controller``.  Replica/hybrid code is explicitly isolated in
``experimental_sonic_replica`` and is disabled by default.
"""
